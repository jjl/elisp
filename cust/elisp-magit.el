; Setup
(add-to-list 'load-path (concat elisp-vendor-dir "magit/"))
(require 'magit)

; Hook
;(defun elisp-magit-custom ()
;  "magit-hook")

;(add-hook 'magit-hook '(lambda() (elisp-magit-custom)))

(provide 'elisp-magit)
